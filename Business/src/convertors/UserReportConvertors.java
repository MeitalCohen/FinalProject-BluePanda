package convertors;

import entities.User;
import entities.reports.UserReportEntity;
import enums.UserStatus;

public class UserReportConvertors {
    /*
    public UserReportEntity convert(User user)
    {
        String userRole = UserStatus.values(user.getUserStatus());
        return new UserReportEntity(user.getId(),user.getUserName(),user.getFirstName(),user.getLastName(),,user.getCreated(),
                ,user.getAddress(),user.getEmail(),user.getPhone());
    }

     */
}
